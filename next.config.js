/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
}

module.exports = {
  images: {
    domains: ['dummyimage.com'],
    domains: ['cdn.discordapp.com'],
  },
  nextConfig
}
